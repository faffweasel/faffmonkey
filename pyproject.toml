[project]
name = "faffmonkey"
version = "0.1.1"
description = "A minimal, self-hosted personal AI agent"
license = "AGPL-3.0-only"
requires-python = ">=3.14"

[tool.pytest.ini_options]
testpaths = ["tests"]
